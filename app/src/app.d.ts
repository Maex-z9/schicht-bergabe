// See https://kit.svelte.dev/docs/types#app
declare global {
  namespace App {
    interface Locals {
      // In nächster Session: supabase: SupabaseClient + user: User | null
    }
    // interface PageData {}
    // interface Error {}
    // interface Platform {}
  }
}

export {};
